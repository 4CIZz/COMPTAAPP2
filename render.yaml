services:
  - type: web
    name: comptaapp2
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run ecritures_comptables2.py --server.port $PORT --server.address 0.0.0.0
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
